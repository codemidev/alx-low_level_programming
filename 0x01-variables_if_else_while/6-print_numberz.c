#include <stdio.h>

/**
 * main - main function
 * Description: Prints  numbers less than 10
 * Return: 0
 */
int main(void)
{
	int x;

	for(x = 0; x <= 10; x++)
		putchar(x);
	putchar('\n');
return (0);
}
