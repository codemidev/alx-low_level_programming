#include <stdio.h>

/**
 * main - main function
 * Description: Prints  numbers less than 10
 * Return: 0
 */
int main(void)
{
	int x;

	for (x = 48; x <= 59; x++)
		putchar(x);
	putchar('\n');
return (0);
}
